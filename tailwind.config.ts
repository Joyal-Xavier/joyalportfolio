Place your static assets here:

- `resume.pdf` — your resume, linked from the Hero section's "Download Resume" button.
- `favicon.ico` — browser tab icon (optional).
- `og-image.png` — 1200x630 image for social share previews (optional, add to metadata in app/layout.tsx if used).
